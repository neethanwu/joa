{
  "name": "joa",
  "version": "0.1.0",
  "type": "module",
  "description": "Journal of Agents — persistent activity journal for AI agents",
  "main": "dist/core/index.js",
  "scripts": {
    "test": "bun test",
    "build": "bun build src/core/index.ts --outdir dist/core --target bun",
    "lint": "bunx biome check .",
    "lint:fix": "bunx biome check --write .",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "ulidx": "^2.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/js-yaml": "^4.0.9",
    "bun-types": "^1.3.9",
    "typescript": "^5.7.0"
  },
  "trustedDependencies": ["@biomejs/biome"]
}
