## Summary

<!-- What does this PR do and why? -->

## Checklist

- [ ] `bun test` passes
- [ ] `bunx biome check .` passes
- [ ] `bunx tsc --noEmit` passes
- [ ] If touching core: `bun run build && node dist/cli/main.js --version` works
